server:
  port: 8081  # Port for this microservice

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/microservice_db
    username: root
    password: Test
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true